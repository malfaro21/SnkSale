export { useColorScheme } from 'react-native';
//The "export" keyword indicates that the following function (useColorScheme) will be exported from the module
//The name "useColorScheme" is the export statement
//from 'react-native': This part specifies the module from which the exported bindings are sourced
